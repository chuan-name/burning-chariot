# 给后续 coding agent 的说明

《燃烧战车》网页复刻版。玩家向的规则说明在 `README.md`，这份文件讲的是**代码怎么组织、
哪些不变量不能破、以及踩过的坑**。改动前请先读完「不变量」和「坑」两节。

---

## 1. 范围

- 复刻新浪游戏大厅代理运营过的回合制战车对战游戏《燃烧战车》。
- **纯前端，零依赖，零构建**：`open index.html` 就能玩。所有脚本用传统 `<script>` 标签
  按顺序加载，共用一个 `window.RZ` 命名空间。不要引入模块打包、npm 依赖或 CDN 资源。
- 单机为主：1v1 / 2v2（打电脑）/ 同屏双人。**没有服务器，也不做联网对战。**
- 代码风格跟现有文件保持一致：ES5 风格的 `var` + `function`，注释用中文，
  说「为什么这么写」而不是复述代码。测试文件里可以用现代语法（只在 Node 跑）。

## 2. 跑起来 / 测试

```sh
open index.html            # 玩
node test/run.js           # 全部无头回归测试（约 190 项，跑完要几分钟）
```

浏览器侧自检页（配合 headless Chrome 截图，见第 6 节）：

```
test/render-check.html?map=sky&n=4&frames=900
test/ui-check.html?stage=title|setup|codex|battle|fire|aimline|pause|enemy|items|itemarmed|ultimate|result
test/perf-check.html?n=200
```

## 3. 架构

```
index.html        界面骨架：标题 / 出战准备 / 图鉴 / 说明 / 战斗，五个 .screen 切换
css/style.css     黑铁机箱 + 琥珀仪表灯
js/data.js        全部数值：战车、武器、地图、道具、常量。改平衡基本只动这里
js/terrain.js     可破坏地形：1bit 掩码 + 同尺寸 ImageData，爆炸只回写脏矩形
js/physics.js     弹道推进与伤害公式。不碰渲染、不改战车状态
js/render.js      背景、战车造型、弹体、粒子。纯绘制，不含规则
js/audio.js       WebAudio 合成音效，无外部音频文件
js/ai.js          电脑：粗扫角度/力度网格 + 局部精搜 + 挖墙模式
js/game.js        规则中枢：轮次、燃料、道具、爆炸结算、镜头
js/main.js        界面装配、输入、主循环。只负责把 game 的状态画进 DOM
test/harness.js   在 Node 里加载浏览器脚本（canvas 用桩件）
test/run.js       回归测试主入口，按主题分节
test/hud-writes.js 计数版 DOM 桩件 + 接管 rAF，测 HUD 每帧写了多少次 DOM、道具点选流程
```

**分层规则**：`physics.js` 不认识 `Game`，`render.js` 不认识规则。
`RZ.step()` 既驱动画面上的炮弹，也被 AI 拿去在一帧内跑上千条弹道——
电脑和玩家用的是**同一套物理**，这条别破坏。

**`main.js` 是个"哑"渲染层**：显示什么由 `game.hudState()`（纯函数）决定，
`main.js` 只负责画。规则判断不要写进 `main.js`，否则没法测。

## 4. 不变量（破了就是 bug）

### 轮次
- **每一轮里每辆车恰好出手一次。** 延迟只决定这一轮内部的先后，不会让谁整轮消失。
- 因此**同一辆车最多连着出手两次**（上一轮压轴 + 下一轮打头）。出现三连就是 bug。
- `unit.delay` 是**赋值**不是累加：`delay = delayFor(u)`，只当下一轮的排序键。
- 平局时「上一轮排在后面的先动」，所以同延迟的两辆车会轮流先手。

### 燃料
- 燃料是**唯一**资源，开炮和移动共用。没有能量槽、没有独立的必杀计量。
- `START_FUEL 22` / `MAX_FUEL 200`：开局只够打一发一号或二号武器加挪两步。
- `延迟 = DELAY_BASE(300) + 本回合耗油 × DELAY_PER_FUEL(5.5)`。
- 每辆车的 `regen` 是**按它自己的 w1 耗油反推**的，保证「打一发一号武器」对谁都净赚
  8~12 点。改武器耗油时必须同步改 regen，否则重型车会入不敷出（见第 5 节）。
- 移动永远不会把油耗到打不出一号武器（`moveActive` 里的 `floor`）。

### 弹道
- 45° 满力度的射程 ≈ 一整张地图宽（1900px），全部 150 套「武器 × 地图」组合都在
  1550~2250px 之间。加新武器要跑一遍射程测试。
- 低重力地图靠 `mapSpeedScale()` 降初速：**射程与常规地图一致**，只有滞空变长。

### 风
- 单回合变化不超过 `windMax × 0.3`；越靠近上下限，回拉越强（三次方）。
- 极端风待不了几个回合。测这条要测「期望位移」，别测随机长尾（见第 5 节）。

### 界面不能泄露信息
- **行动顺序不显示**：没有出手队列、不显示延迟、不提示「让对手打几手」。
  这是玩家自己该记的信息，`test/hud-writes.js` 里有守卫。
- **对手的读数不显示**：轮到电脑（或 2v2 的电脑队友）时，下方面板退回显示玩家自己的车
  并置灰。判断逻辑全在 `game.hudState()` / `viewUnit()` 这两个纯函数里。
- **用道具不公开用了哪个**：车身上只亮通用的「道具」标记，播报里也不能出现道具名。
- **隐身对电脑同样生效**：AI 只能通过 `game.knownPos(team, unit)` 拿位置，
  隐身期间返回的是 `lastSeen`。任何新增的 AI 逻辑都必须走这个函数，不许读 `u.x`。

### 性能
- `updateHUD()` 每帧都跑，但**所有 DOM 写入都要先比对缓存**（`setText/setStyle/setHTML/...`）。
  稳定态必须接近 0 次写入/帧。曾经每帧无条件写 23 次（含两次 innerHTML 重建 +
  一次风向仪画布重绘），肉眼可见地掉帧。
- 别在会频繁变动的 HUD 子树上加 CSS `filter`：会强制整块重新栅格化。用 `opacity`。
- 动画是**按帧**推进的（蓄力、弹速都是每帧固定增量），所以掉帧 = 整个游戏变慢。

### 道具
- 只能在**开炮之前**用；一回合可以连用多个；`endsTurn` 的用完立刻交出回合。
- **带回合数的状态在「自己这一回合结束时」递减**（`endTurn` 里），不是回合开始。
  放回合开始会少算一整回合。
- 双倍攻击是**真的打两发**（第二发等第一发的坑炸完才出膛，所以两发会互相改地形），
  不是伤害 ×2；对必杀无效。
- 伤害倍率挂在**弹体**上（`p.dmgMul`），这样双倍攻击的两发都能吃到加强攻击的 +50%。

## 5. 踩过的坑

按被坑的惨烈程度排序，改代码前扫一眼：

1. **`result === 0` 是假值。** 红队获胜时 `game.result = 0`，`if (this.result)` 全部失效，
   对局判定结束后还在继续跑回合。所有地方一律用 `!== null`。
2. **带回合数的状态别在回合开始递减。** 挂 3 回合的麻痹只困住了对方 2 回合。
3. **headless Chrome 的 `requestAnimationFrame` 只跑两帧，且时机不定。**
   浏览器里测不了任何逐帧行为。凡是每帧刷新的东西都做成纯函数放 Node 里测
   （`game.applyHeld()`、`game.hudState()`），或者用 `test/hud-writes.js` 那套
   「接管 rAF 自己驱动主循环」的桩件。截图倒是可靠（在 virtual time 末尾成像）。
4. **"完全封死的墙"不存在**：炮弹可以从世界上方（y<0）掠过任何地形。
   别写依赖"密不透风"的测试场景——曾经因此误判 AI 有 bug，其实是测试写错了。
5. **地形只存在于地图范围内**：从地图边缘打满射程的炮弹永远不会落地，
   量射程要用「回到出膛高度时走了多远」，不能等它撞地。
6. **随机量别测长尾。** 「极端风最多连续几回合」「深海遗迹最长多少回合」这类断言
   会偶发失败。要么测期望值/占比，要么把阈值放到病灶量级（131 回合 → 阈值 90）。
7. **改武器耗油要连着改 regen。** 曾经奥丁 w1 耗油 14、回油只有 10，
   每回合净亏 2 点，连挪一步都做不到。`test/run.js` 的「燃料经济」一节有守卫。
8. **AI 每回合思考约 23ms**（一帧多一点），这是可接受的一次性卡顿。
   再往里加网格搜索前先测一下，别累加成肉眼可见的顿挫。
9. **回合数是重尾分布**：深海遗迹上低攻打高防（比如贝贝龙对铁金刚）实测
   中位 24、p95 43，偶尔能摸到 110 回合；双方都带道具互相回血时更长
   （实测中位 27、p95 75、最长 119）。所以测试里只硬性要求「必须分出胜负」，
   长度用均值这类稳定统计量来卡，别对最大值设紧阈值——会偶发失败。
10. **僵局**：低攻打高防、中间又隔着地形时会磨很久。三重保险：
   AI 打不中时进入挖墙模式（`digPlan`）、评分对 0 伤害的落点按「离敌人多近」打分、
   `game.turnsSinceDamage >= 5` 时强制随机换位。改 AI 移动逻辑时别把这些拆了。
11. **别让 AI 偷看**：难度只影响瞄准抖动（`jitterAim/jitterPow`）和是否总选最优解，
    不给任何额外情报。隐身、道具都要对 AI 同样生效。

## 6. 用 headless Chrome 看画面

```sh
CH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
"$CH" --headless=new --disable-gpu --hide-scrollbars --allow-file-access-from-files \
      --window-size=1280,760 --virtual-time-budget=12000 --screenshot=out.png \
      "file://$PWD/test/ui-check.html?stage=battle"
```

- `--allow-file-access-from-files` 是必须的，`ui-check.html` 要操作 iframe 里的游戏。
- `--dump-dom` 只能拿到顶层文档，拿不到 iframe 里的内容；要查 DOM 就直接开 `index.html`。
- `perf-check.html` 在 headless 下测不准（不做真正的光栅化），只有真实 GPU 环境有意义。

## 7. TODO / 已知缺口

- **电脑不会用「飞行」道具**：挑一个好的传送落点是另一套搜索问题，没做。
  其余八种道具 AI 都会用（`aiUseSupportItems` / `aiUseOffenseItems`）。
- **没有金币经济**：赛前道具不限量。原作里道具要花金币、金币靠赢比赛攒，
  将来接的话入口在 `main.js` 的 `buildShop()` 和 `setup.loadout`。
- **定位炮 / 麻痹弹是固定耗油**（8 / 10），与当时选中的武器无关。原作可能是按选中武器算。
- **道具图标是文字**，没有美术资源；`战车图鉴` 页面也还没收录道具。
- **触屏**：道具栏能点，但没在真机上验证过；触屏按钮行里也没有「跳过回合」。
- **没有存档/成长**：每局都是全新开始。
- **道具会拉长比赛**：双方都带道具时，深海遗迹的对局均长从 25 回合涨到 35，
  p95 从 43 涨到 75。主要是双方轮流回血。如果想收一收，旋钮在
  `game.js` 的 `aiUseSupportItems()`（电脑回血的血量阈值）或道具的回复比例。
- **同屏双人**共用一套选车/选道具流程（`setup.pickSlot` 在两位玩家间切换），
  但战斗中不会遮挡对方的道具栏——同屏本来也没有秘密可言。
